# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg
dist/
build/

# Django
db.sqlite3
db.sqlite3-journal
staticfiles/
media/

# Environment
.env
.env.*
!.env.example

# IDE
.vscode/
.idea/
*.swp
*.swo

# Testing
.coverage
htmlcov/
.pytest_cache/
test-results/
.playwright-mcp/

# Type checking
.mypy_cache/

# Virtual environments
.venv/
venv/

# Fixtures with personal data - load manually, never commit
membership/fixtures/*.json

# Local dev artifacts
django.log
.superpowers/
.ruff_cache/
# Local Claude Code state stays out; the shared commands and skills travel with the repo.
.claude/*
!.claude/commands/
!.claude/skills/
db.sqlite3.bak*
docker-compose.override.yml

# WSL NTFS alternate-data-stream sidecars
*:Zone.Identifier

# SEO audit reports and other PDF exports - not source
*.pdf

# CMS copy-review screenshots (generated by scripts/capture-cms-screenshots.sh)
screenshots/
# ...but PR screenshots and mockups are committed on purpose (CONTRIBUTING.md)
!mockups/screenshots/

# Empty local test DB artifacts
test_db.sqlite3
test.sqlite3
# Scratch DB for targeted pytest runs (CLAUDE.md's DATABASE_URL=sqlite:///tmp-check.sqlite3)
tmp-check.sqlite3*

# Email copy gallery build output (scripts/build-email-gallery.sh)
email-gallery/
